# ADK runtime API base URL (Cloud Run URL or local API)
ADK_API_BASE_URL=http://localhost:8000

# Upstream request timeout in ms
ADK_API_TIMEOUT_MS=9000

# Optional default run to open if run_id is not provided to open_run_player
ADK_DEFAULT_RUN_ID=
